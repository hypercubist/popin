<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.summer.popin.domain.dao.MemberMapper">
    <select id="findByNo" resultType="MemberVO" parameterType="Long">
        select * from member where no=#{no}
    </select>
</mapper>